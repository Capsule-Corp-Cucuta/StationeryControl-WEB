import { CertificateStatePipe } from './certificate-state.pipe';

describe('CertificateStatePipe', () => {
  it('create an instance', () => {
    const pipe = new CertificateStatePipe();
    expect(pipe).toBeTruthy();
  });
});
